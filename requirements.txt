# Pinned for BAAI/EVA-CLIP-18B remote code (early-2024 vintage) and the
# HF script-based ImageNet loader:
#   - transformers >=4.50 risks private-API drift against the remote code
#   - datasets >=3.0 removed script-based datasets (breaks ILSVRC/imagenet-1k)
torch>=2.1
torchvision>=0.16
transformers>=4.40,<4.50
datasets>=2.14,<3
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
Pillow>=9.0
pytest>=7.0
